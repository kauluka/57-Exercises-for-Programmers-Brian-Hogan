# 2. Counting the Number of Characters
input_string = input("What is the input string? ")
input_string_length = len(input_string)

print(f"{input_string}  has " + str(input_string_length)+ " characters.")
